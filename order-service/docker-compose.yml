services:
  order-service:
    build: .
    container_name: order-service
    ports:
      - "8001:8000"
    networks:
      - restaurant-network

networks:
  restaurant-network:
    name: restaurant-network